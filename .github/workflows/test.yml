name: Tests

on:
  push:
    branches: [main, develop, project3-cicd-pipeline]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov
      
      - name: Run unit tests
        run: pytest projects/project3-cicd-pipeline/tests/unit/ -v
      
      - name: Run integration tests
        run: pytest projects/project3-cicd-pipeline/tests/integration/ -v
      
      - name: Run all tests with coverage
        run: pytest projects/project3-cicd-pipeline/tests/ -v --cov=projects/project3-cicd-pipeline --cov-report=xml
      
      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        if: always()